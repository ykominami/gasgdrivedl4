export class Itemx {
  index: number;
  name: string;
  constructor(options: { index: number, name: string }) {
    this.index = options.index;
    this.name = options.name;
  }
}
// export default Item;